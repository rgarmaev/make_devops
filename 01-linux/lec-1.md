# Лекция 1: Знакомство с Linux и основы командной строки 🐧

## Содержание
1. [Что такое Linux?](#что-такое-linux)
2. [Философия Unix/Linux](#философия-unixlinux)
3. [Дистрибутивы Linux](#дистрибутивы-linux)
4. [Подключение к серверу](#подключение-к-серверу)
5. [Первые шаги в терминале](#первые-шаги-в-терминале)
6. [Файловая система Linux](#файловая-система-linux)
7. [Навигация и работа с файлами](#навигация-и-работа-с-файлами)
8. [Практическое задание](#практическое-задание)

---

## Что такое Linux? 🤔

Linux — это семейство операционных систем с открытым исходным кодом, построенных на ядре Linux. В контексте DevOps мы встречаемся с Linux почти всегда, потому что:

- **Надежность** — серверы могут работать годами без перезагрузки
- **Безопасность** — гибкая система прав доступа
- **Производительность** — минимальное потребление ресурсов
- **Гибкость** — можно настроить под любые задачи
- **Бесплатность** — большинство дистрибутивов совершенно бесплатны

> 💡 **Важно:** В мире DevOps 99% серверов работают именно на Linux. Windows Server встречается, но значительно реже.

## Философия Unix/Linux 📜

Linux унаследовал философию Unix, которая строится на нескольких принципах:

1. **Всё есть файл** — даже устройства и процессы представлены как файлы
2. **Одна программа — одна задача** — программы делают одну вещь, но делают её хорошо
3. **Текстовые конфигурации** — всё настраивается через текстовые файлы
4. **Модульность** — сложные задачи решаются комбинацией простых программ

## Дистрибутивы Linux 📦

Linux — это только ядро. Дистрибутив — это ядро + набор программ + менеджер пакетов.

### Основные семейства:

| Семейство | Менеджер пакетов | Популярные дистрибутивы | Где используется |
|-----------|------------------|------------------------|-------------------|
| **Debian** | `apt` | Ubuntu, Debian | Веб-серверы, разработка |
| **Red Hat** | `yum`/`dnf` | CentOS, RHEL, Fedora | Корпоративный сектор |
| **Arch** | `pacman` | Arch Linux, Manjaro | Энтузиасты, "своя сборка" |
| **SUSE** | `zypper` | openSUSE, SLES | Корпоративный сектор |

**Для DevOps чаще всего выбирают:**
- **Ubuntu LTS** (Long Term Support) — для разработки и многих проектов
- **CentOS/Rocky Linux/AlmaLinux** — для корпоративных решений
- **Debian** — для максимальной стабильности

## Подключение к серверу 🔌

### Локальный доступ
Если Linux установлен на вашем компьютере, открываем **терминал**:
- `Ctrl + Alt + T` (в большинстве окружений)
- Ищем в меню приложений "Terminal", "Konsole", "GNOME Terminal"

### Удаленный доступ через SSH
Для управления серверами используется протокол SSH (Secure Shell):

```bash
# Базовая команда подключения
ssh username@ip_address

# Пример: подключаемся к серверу
ssh root@192.168.1.100

# Если нужен другой порт (по умолчанию 22)
ssh -p 2222 user@server.com

# Использование ключа вместо пароля
ssh -i ~/.ssh/mykey.pem ec2-user@aws-server.com
```

**Первый вход:**
- При первом подключении спросят подтверждение ключа хоста (вводим `yes`)
- Затем вводим пароль (символы не отображаются в целях безопасности)

> 🔐 **Совет по безопасности:** Отключайте вход по паролю и используйте SSH-ключи!

## Первые шаги в терминале ⌨️

### Структура командной строки
```
[user@hostname working_directory]$
[user@hostname working_directory]#
         ↑                           ↑
      обычный пользователь        root (админ)
```

**Разбор строки:**
- `user` — имя текущего пользователя
- `hostname` — имя компьютера/сервера
- `working_directory` — текущая папка (`~` означает домашнюю директорию)
- `$` или `#` — приглашение ввода (`#` означает root-права)

### Базовая структура команды
```bash
команда [опции] [аргументы]

# Примеры:
ls -la /home
    ↑   ↑    ↑
 команда опции аргумент

# Опции бывают:
ls -l     # короткая опция (одна буква)
ls --all  # длинная опция (слово)
```

### Первые команды

```bash
# Кто я?
whoami

# Где я?
pwd     # print working directory

# Что здесь есть?
ls      # list directory contents
ls -la  # подробный список, включая скрытые файлы

# Очистить экран
clear   # или Ctrl + L

# Посмотреть дату и время
date

# Кто ещё в системе?
who
w       # более подробная информация

# Информация о системе
uname -a           # информация о ядре
hostnamectl        # информация о хосте (в systemd)
```

## Файловая система Linux 📁

### Стандартная иерархия (FHS - Filesystem Hierarchy Standard)

```
/                       # Корень файловой системы
├── bin/                # Основные исполняемые файлы (команды)
├── boot/               # Файлы загрузчика и ядро
├── dev/                # Файлы устройств
├── etc/                # Конфигурационные файлы
├── home/               # Домашние папки пользователей
├── lib/                # Системные библиотеки
├── media/              # Точки монтирования съемных носителей
├── mnt/                # Временное монтирование
├── opt/                # Дополнительное ПО
├── proc/               # Виртуальная файловая система процессов
├── root/               # Домашняя папка root
├── sbin/               # Системные команды (для администрирования)
├── tmp/                # Временные файлы
├── usr/                # Пользовательские программы и данные
│   ├── bin/            # Пользовательские программы
│   ├── lib/            # Библиотеки для программ
│   └── share/          # Общие данные
└── var/                # Изменяемые данные (логи, кеш)
    ├── log/            # Логи системы
    └── tmp/            # Временные файлы между перезагрузками
```

### Важные директории для DevOps:

| Директория | Назначение |
|------------|------------|
| `/etc` | Конфиги всего ПО (nginx.conf, ssh/sshd_config) |
| `/var/log` | Все логи системы и приложений |
| `/home` | Домашние папки разработчиков |
| `/tmp` | Временные файлы (очищается при перезагрузке) |
| `/opt` | Часто ставят сюда свои приложения |

## Навигация и работа с файлами 🧭

### Перемещение по директориям

```bash
# Перейти в директорию
cd /home/user
cd Documents
cd ..              # на уровень выше
cd ../..           # на два уровня выше
cd ~               # в домашнюю директорию
cd -               # в предыдущую директорию

# Вернуться домой (несколько способов)
cd
cd ~
cd $HOME
```

### Просмотр содержимого

```bash
# Простой список
ls

# Подробный список
ls -l

# Все файлы (включая скрытые)
ls -a

# Человеко-читаемые размеры
ls -lh

# Сортировка по времени изменения
ls -lt

# Рекурсивно (включая поддиректории)
ls -R

# Можно комбинировать опции
ls -la
ls -lhtr  # подробно, человеко-читаемо, по времени, в обратном порядке
```

### Работа с файлами и директориями

```bash
# Создание файлов
touch file.txt                    # создать пустой файл
touch file{1..5}.txt              # создать file1.txt ... file5.txt
echo "Hello" > file.txt           # создать файл с содержимым
cat > file.txt                    # ввод с клавиатуры (Ctrl+D для сохранения)

# Создание директорий
mkdir newdir
mkdir -p path/to/nested/dir       # создать всю цепочку директорий

# Копирование
cp file.txt copy.txt              # копировать файл
cp -r dir1 dir2                    # копировать директорию рекурсивно

# Перемещение/переименование
mv file.txt newname.txt           # переименовать
mv file.txt dir/                   # переместить в директорию

# Удаление (осторожно!)
rm file.txt                        # удалить файл
rm -f file.txt                     # принудительно (без подтверждения)
rm -r dir/                          # удалить директорию рекурсивно
rm -rf dir/                         # удалить директорию без вопросов (опасно!)
rm -rf /                            # НИКОГДА ТАК НЕ ДЕЛАЙТЕ! 😱

# Просмотр содержимого файлов
cat file.txt                       # вывести весь файл
less file.txt                       # постраничный просмотр (q для выхода)
head file.txt                       # первые 10 строк
head -n 20 file.txt                  # первые 20 строк
tail file.txt                       # последние 10 строк
tail -f /var/log/syslog             # следить за изменениями файла (логи)
```

### Работа с текстом

```bash
# Поиск в файлах (grep)
grep "error" log.txt               # найти строки с "error"
grep -i "error" log.txt            # без учета регистра
grep -r "TODO" ./src               # рекурсивный поиск в директории

# Подсчет строк, слов, символов
wc -l file.txt                      # количество строк
wc -w file.txt                      # количество слов
wc -c file.txt                      # количество символов

# Сортировка
sort file.txt                       # отсортировать строки
sort -r file.txt                    # в обратном порядке
sort -n numbers.txt                  # числовая сортировка

# Уникальные значения
uniq file.txt                       # удалить повторяющиеся строки
sort file.txt | uniq -c             # посчитать повторения
```

## Полезные комбинации клавиш ⌨️

```bash
# Управление процессами
Ctrl + C             # прервать текущую команду
Ctrl + Z             # приостановить команду (можно вернуть через fg)

# Навигация в командной строке
Ctrl + A             # в начало строки
Ctrl + E             # в конец строки
Ctrl + U             # удалить от курсора до начала строки
Ctrl + K             # удалить от курсора до конца строки
Ctrl + W             # удалить слово до курсора

# История команд
↑ / ↓                # навигация по истории
Ctrl + R             # поиск по истории (введите часть команды)
history              # показать всю историю
!!                   # выполнить последнюю команду
!100                 # выполнить команду под номером 100 из истории

# Остальное
Tab                  # автодополнение (путей и команд)
Tab + Tab            # показать все варианты
Ctrl + L             # очистить экран (как clear)
Ctrl + D             # завершить сессию (выйти из shell)
```

## Практическое задание 📝

### Задание 1: Исследование системы
```bash
# Выполните команды и запишите, что они показывают:
whoami
pwd
hostname
uname -a
lsb_release -a      # информация о дистрибутиве (на Ubuntu)
cat /etc/os-release # информация о системе
```

### Задание 2: Навигация
```bash
# 1. Перейдите в корневую директорию
cd /

# 2. Посмотрите, что там есть
ls -la

# 3. Перейдите в /etc и найдите файл с информацией о системе
cd /etc
ls -l | grep release

# 4. Вернитесь домой
cd ~
```

### Задание 3: Работа с файлами
```bash
# 1. Создайте структуру директорий
mkdir -p ~/devops-practice/{linux,git,docker}/{notes,projects}

# 2. Создайте несколько файлов
touch ~/devops-practice/linux/notes/first-lesson.md
echo "Мои первые шаги в Linux" > ~/devops-practice/linux/notes/first-lesson.md

# 3. Скопируйте файл
cp ~/devops-practice/linux/notes/first-lesson.md \
   ~/devops-practice/linux/notes/backup.md

# 4. Посмотрите содержимое
cat ~/devops-practice/linux/notes/first-lesson.md

# 5. Переименуйте backup.md в lesson1-backup.md
mv ~/devops-practice/linux/notes/backup.md \
   ~/devops-practice/linux/notes/lesson1-backup.md
```

### Задание 4: Поиск и фильтрация
```bash
# 1. Найдите все файлы .conf в /etc
ls -R /etc 2>/dev/null | grep "\.conf$"

# 2. Посмотрите последние 20 строк системного лога
tail -20 /var/log/syslog 2>/dev/null || tail -20 /var/log/messages

# 3. Найдите свой процесс bash
ps aux | grep bash
```

### Задание 5: Работа с историей
```bash
# 1. Посмотрите историю ваших команд
history

# 2. Найдите в истории команды с mkdir
history | grep mkdir

# 3. Выполните команду из истории (укажите свой номер)
!номер_команды
```

## Вопросы для самопроверки 🤔

1. Что означает символ `~` в Linux?
2. Чем отличается абсолютный путь от относительного?
3. Для чего нужна директория `/etc`?
4. Что произойдет при выполнении `rm -rf /`?
5. Как посмотреть список скрытых файлов?
6. Чем отличается `>` от `>>`?
7. Как найти все файлы с расширением .log в системе?

## Дополнительные материалы 📚

- [Linux Command Line Basics](https://ubuntu.com/tutorials/command-line-for-beginners)
- [Explain Shell](https://explainshell.com/) — объясняет любую команду
- [Linux Journey](https://linuxjourney.com/) — интерактивное обучение
- [Cheat Sheet](https://www.guru99.com/linux-commands-cheat-sheet.html) — шпаргалка по командам

---

**Что дальше?** В следующей лекции мы подробно разберем права доступа, управление пользователями и процессы в Linux. А пока — практикуйтесь! 💪

> 🐧 **Совет дня:** "Не бойтесь экспериментировать. Лучший способ выучить Linux — использовать его каждый день. Создайте виртуальную машину и делайте все задания в ней — если что-то сломаете, всегда можно переустановить!"